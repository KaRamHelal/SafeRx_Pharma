---
title: Postman collection
subtitle: Exercise signed Enterprise requests
---

The repository collection is a request-shape example, not a credential store. Set `base_url` and inject the key through a local secret variable. The signing headers must be generated for each request; a static signature is not reusable because the timestamp and nonce are part of the signed message.

For repeatable automation, prefer the signing-aware SDKs.
